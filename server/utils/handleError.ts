// This is a placeholder for a more robust error logging
// Could post to error monitoring or logging like Cloudwatch or Sentry
function handleError(error) {
  console.error(error);
}

export default handleError;
